[project]
name = "corpus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.28.1",
    "protego>=0.5.0",
    "pydantic>=2.13.4",
    "pytest>=8.4.2",
    "pyyaml>=6.0.3",
    "typer>=0.23.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
